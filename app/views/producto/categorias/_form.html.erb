<%= form_with(model: producto_categoria, local: true) do |form| %>
  <% if producto_categoria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(producto_categoria.errors.count, "error") %> prohibited this producto_categoria from being saved:</h2>

      <ul>
      <% producto_categoria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :categoria %>
    <%= form.text_field :categoria, id: :producto_categoria_categoria %>
  </div>

  <div class="field">
    <%= form.label :activo %>
    <%= form.check_box :activo, id: :producto_categoria_activo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
