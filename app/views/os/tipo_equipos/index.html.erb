<p id="notice"><%= notice %></p>

<h1>Os Tipo Equipos</h1>

<table>
  <thead>
    <tr>
      <th>Tipo</th>
      <th>Activo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @os_tipo_equipos.each do |os_tipo_equipo| %>
      <tr>
        <td><%= os_tipo_equipo.tipo %></td>
        <td><%= os_tipo_equipo.activo %></td>
        <td><%= link_to 'Show', os_tipo_equipo %></td>
        <td><%= link_to 'Edit', edit_os_tipo_equipo_path(os_tipo_equipo) %></td>
        <td><%= link_to 'Destroy', os_tipo_equipo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Os Tipo Equipo', new_os_tipo_equipo_path %>
