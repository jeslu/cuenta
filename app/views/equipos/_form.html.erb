<%= form_with(model: equipo, local: true) do |form| %>
  <% if equipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipo.errors.count, "error") %> prohibited this equipo from being saved:</h2>

      <ul>
      <% equipo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :n_serie %>
    <%= form.text_field :n_serie, id: :equipo_n_serie %>
  </div>

  <div class="field">
    <%= form.label :marca_id %>
    <%= form.text_field :marca_id, id: :equipo_marca_id %>
  </div>

  <div class="field">
    <%= form.label :modelo_id %>
    <%= form.text_field :modelo_id, id: :equipo_modelo_id %>
  </div>

  <div class="field">
    <%= form.label :tipo_equipo_id %>
    <%= form.text_field :tipo_equipo_id, id: :equipo_tipo_equipo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
