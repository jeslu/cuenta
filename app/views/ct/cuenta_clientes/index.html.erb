<p id="notice"><%= notice %></p>
<div class="card">
  <div class="card-body">


<%= render 'form', ct_cuenta_cliente: @ct_cuenta_cliente %>
  </div>
</div>


<div class="card">
  <div class="card-header">
    <h1> Cuenta de Clientes</h1>
  </div>

<div class="table-responsive-sm">


<table class="table">
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Fecha c</th>
      <th>Activo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ct_cuenta_clientes.each do |ct_cuenta_cliente| %>
      <tr>
        <td><%= ct_cuenta_cliente.Cliente.nombre %></td>
        <td><%= ct_cuenta_cliente.fecha_c.to_date %></td>
        <td><%= ct_cuenta_cliente.activo %></td>
        <td><%= link_to 'Show', ct_cuenta_cliente %></td>
        <td><%= link_to 'Edit', edit_ct_cuenta_cliente_path(ct_cuenta_cliente) %></td>
        <td><%= link_to 'Destroy', ct_cuenta_cliente, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
<br>
