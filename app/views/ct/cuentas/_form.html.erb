<%= form_with(model: ct_cuenta, local: true) do |form| %>
  <% if ct_cuenta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ct_cuenta.errors.count, "error") %> prohibited this ct_cuenta from being saved:</h2>

      <ul>
      <% ct_cuenta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.collection_select(:cliente_id, Cliente.all, :id, :nombre, options = {}, {:class => "select"})%>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha, id: :ct_cuenta_fecha %>
  </div>

  <div class="field">
    <%= form.label :activo %>
    <%= form.check_box :activo, id: :ct_cuenta_activo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
