<%= form_with(model: ct_cuentum, local: true) do |form| %>
  <% if ct_cuentum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ct_cuentum.errors.count, "error") %> prohibited this ct_cuentum from being saved:</h2>

      <ul>
      <% ct_cuentum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id, id: :ct_cuentum_cliente_id %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha, id: :ct_cuentum_fecha %>
  </div>

  <div class="field">
    <%= form.label :activo %>
    <%= form.check_box :activo, id: :ct_cuentum_activo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
